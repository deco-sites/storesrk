import Component from "$store/components/form/Form.tsx";
import type { Props } from "$store/components/form/Form.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
